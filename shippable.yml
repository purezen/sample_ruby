language: ruby
rvm:
  - 2.0.0
  - 2.1.0

matrix:
  exclude:
    - rvm: 2.0.0
      env: ISOLATED=false

install: shippable_retry 'if [ $i -eq 3 ]; then echo "done"; else foobar$i; fi'
  
script: true
